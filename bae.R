# BAE 假设

# ===================== 1 通用假设 =================

# --------------------- 投资收益率假设 ----------------

#  用于内含价值(EV)、新业务价值(VNB)、
#       毛保费准备金(GPV)和现金流预测的投资收益率假设

bae.inv_rate_df = data.frame(c()) 